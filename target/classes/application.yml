spring:
  application:
    name: javaai
  ai:
    openai:
      # DeepSeek API Key
      api-key: sk-75262ec5d9544471890532d12d6b2d32
      
      # ==========================================
      # 切换大模型时，只需修改下面激活的 base-url 和 model
      # ==========================================
      
      # 【当前激活：DeepSeek (深度求索)】
      base-url: https://api.deepseek.com
      chat:
        options:
          # deepseek-chat 是标准的对话模型
          model: deepseek-chat
          temperature: 0.7

server:
  port: 8080
